{
  "name": "ga4-clone-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm run start -w @ga4-clone/backend",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
